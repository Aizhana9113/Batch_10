package Array;

import java.util.Arrays;

public class ForEachLoop {
    public static void main(String[] args) {
        /*int number[] = {45, 5, 9, 12, 56, 0, 30};
        System.out.println(Arrays.toString(number));

        String[] colors = {"Red", "Yellow", "brown", "White", "Orange", "Pink", "Green", "Black", "aAbc", "aBac", "12", "34"};
        //it will show first numbers than string than lower case than
        Arrays.sort(colors);
        for (String color : colors) {// this is Each loop---> it will show all of my size in the array
            System.out.println(color);

            int[] ids = {9, 5, 8, 23, 45, 12, 50, 30, 100, 11};
            // use for each loop to print out this elements from int array

            for (int number : ids) {
                //System.out.println(number);

                if (number >= 50) {
                    System.out.println(number);
                }

            }
            // print out only letters from the char array
            char[] character = {'g', '1', 5, '$', '*', 'A', 'H'};
            for (char ch : character) {
                if ((ch >= 'a' && ch <= 'z') || (ch >= 'a' && ch <= 'z')) {
                    System.out.println(ch);
                }
            }


            for (char ch : character) {
                Character.isDigit(ch);
                Character.isAlphabetic(ch);
                if (Character.isAlphabetic(ch) || Character.isDigit(ch)) {
                    System.out.println(ch);
                }
            }

         */

        }
    }